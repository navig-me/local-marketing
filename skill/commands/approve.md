---
description: Approve a pending draft so it can send
argument-hint: <path to a file in pending_review/>
---

Run `npx @navig-me/local-marketing approve $ARGUMENTS`. If no path was
given, first run `npx @navig-me/local-marketing review` to show me what's
pending and ask which one to approve.
